[dependencies]
serde = { workspace = true, optional = true, features = ["derive"] }
swc_atoms = { version = "3.1.0", path = "../swc_atoms" }
swc_common = { version = "6.1.1", path = "../swc_common" }
swc_html_ast = { version = "6.0.0", path = "../swc_html_ast" }
swc_visit = { version = "2.0.0", path = "../swc_visit" }

[features]
default = ["serde"]
path = []
serde-impl = ["serde"]

[lib]
bench = false
path = "Source/lib.rs"

[package]
authors = [
  "강동윤 <kdy1997.dev@gmail.com>",
  "Alexander Akait <sheo13666q@gmail.com>",
]
description = "Visitor for html"
documentation = "https://rustdoc.swc.rs/swc_html_visit/"
edition = "2024"
license-file = { workspace = true }
name = "swc_html_visit"
repository = "https://github.com/swc-project/swc.git"
version = "7.0.0"

[package.metadata]

[package.metadata.docs]

[features]
default    = ["serde"]
path       = []
serde-impl = ["serde"]

[dependencies]
serde = { workspace = true, optional = true, features = ["derive"] }

swc_atoms    = { version = "4.0.0", path = "../swc_atoms" }
swc_common   = { version = "7.0.0", path = "../swc_common" }
swc_html_ast = { version = "7.0.0", path = "../swc_html_ast" }
swc_visit    = { version = "2.0.0", path = "../swc_visit" }
